@model IEnumerable<Tag>

<textarea id="Tags" name="Tags" style="height:200px; width:300px;">
    @if (Model.SafeAny())
    {
        @String.Join(", ", Model.Select(tag => tag.Name))
    }
</textarea>
