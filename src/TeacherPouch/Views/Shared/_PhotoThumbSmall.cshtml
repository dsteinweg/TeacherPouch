@model Photo

<a asp-route="photo-details"
   asp-route-id="@Model.Id"
   asp-route-name="@Model.Name"
   class="photo-thumb">
    <img src="@Url.Action("Image", "Photo", new { id = Model.Id, size = PhotoSizes.Small, fileName = Model.Name + "_Small.jpg" })">
</a>